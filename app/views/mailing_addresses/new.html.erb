<% form_for :mailing_adress, :url => mailing_addresses_path  do |form| %>
<%= form.error_messages %>
	<p>
		<%= form.label :email %><br />
		<%= form.text_field :email %>
	</p>

		<%= form.submit t('newsletter.subscribe_long') %>
	</p>

<% end %>