<% if current_user.is_following?(@user) %>
	<%= link_to "Unfollow", friendship_path(@user.id), :method => :delete %>
<% else %>
	<%= link_to "Follow", friendships_path(:friend_id => @user), :method => :post %> 
<% end %>