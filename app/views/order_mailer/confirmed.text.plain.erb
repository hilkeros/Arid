Thanks for your order.

Please transfer <%= @order.total_price + 10.0.to_f %> to our bank account.
"Arid Merchandise - KBC 731-0094471-65"
Please mention the order code "<%= @order.payment_code %>"

We will ship your order after we've received your payment.

Your order information:
<% @order.order_products.each do |order_product| %>
	<%= order_product.product.name %> (&euro;<%= order_product.price %>)
	<%= "Selected variant: #{order_product.variant}" if order_product.variant.present? %>
<% end %>

Total order price
Total order: &euro;<%= @order.total_price %>
Shipping costs: &euro; 10
Total: &euro;<%= @order.total_price + 10.to_f %>

Billing information
<%= @order.billing_address.name %>
<%= raw @order.billing_address.address %>

Shipping information
<%= @order.shipping_address.name %>
<%= raw @order.shipping_address.address %>


regards
Arid.