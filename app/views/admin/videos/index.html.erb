<div class="block">
  <div class="secondary-navigation">
    <ul>
      <li class="first active"><%= link_to 'List', admin_videos_path %></li>
      <li><%= link_to 'New', new_admin_video_path %></li>
    </ul>
    <div class="clear"></div>
  </div>          
  <div class="content">          
    <h2 class="title">All Videos</h2>
    <div class="inner">
      <table class="table">
        <tr>             
          <th class="first">ID</th>
										<th>Url</th>
					          <th>Created at</th>
          <th class="last">&nbsp;</th>
        </tr>
        <% @videos.each do |video| -%>
        <tr class="<%= cycle("odd", "even") %>">
          <td>
            <%= video.id %>
          </td>
										<td>
						<%= link_to video.url, admin_video_path(video) %>
					</td>
										<td>
            <%= video.created_at %>
          </td>
          <td class="last">
            <%= link_to 'show', admin_video_path(video) %> |
            <%= link_to 'edit', edit_admin_video_path(video) %> |
            <%= link_to 'destroy', admin_video_path(video), :method => :delete, :confirm => 'Are you sure?' %>
          </td>
        </tr>          
        <% end -%>
      </table>
      <div class="actions-bar">
        <div class="actions">
        </div>
        
        <div class="clear"></div>
      </div>
    </div>
  </div>
</div>

<% content_for :sidebar, render(:partial => 'sidebar') -%>